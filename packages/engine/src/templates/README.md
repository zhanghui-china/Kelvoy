# templates/

叙事骨架模板(FR-02)+ 用户私有模板(FR-10)的存放位置。

FR-02 列出的六种目的地类型骨架，内容见 `skeletons.ts`（M1-10）：结构性的段落划分 +
镜头类型配比，不含具体地标名——具体地标/美食/动线来自 `Destination` 记录，由脚本
provider 的 prompt 把两者拼起来。

用户可把骨架、LUT、片头片尾另存为私有模板(FR-10),这部分存储/发现机制也待定。
