line-length = 100
target-version = "py312"
extend-exclude = ["spike", "scripts", "dgx-bridge"]

[lint]
select = ["E", "F", "I", "B"]

[lint.flake8-bugbear]
extend-immutable-calls = ["fastapi.Depends"]
