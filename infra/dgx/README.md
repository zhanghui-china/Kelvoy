# infra/dgx/

PRD v0.2 的"两台 DGX Spark"确认成立,但**不是两台对等独立的机器**:一台(`gx10-8e22`)和 visionary、shanhai 共用;另一台(`spark-c327`)是 Kelvoy 专属,连接信息 2026-09-24 已拿到,见下方。§9"一台宕机容量减半但不停服"这条要按这个不对等的情况重新想——共享那台平时就要和别的项目抢 GPU,不是"两台对等、坏一台各分担一半"那么简单。

## 硬件与现状(2026-09-23 实地侦察,只读,共享的这台)

- 主机名 `gx10-8e22`,Tailscale IP `100.80.224.95`。SSH:`ssh -i ~/.ssh/visionary_dgx huntun@100.80.224.95`(与 visionary 项目共用账号/密钥,经确认有权限使用)。
- GPU:NVIDIA GB10,驱动 580.126.09,CUDA 13.0。内核 `6.17.0-1008-nvidia`,aarch64。
- 内存:119GB,当前可用 ~106GB。
- **磁盘:916GB,已用 829GB(96%),只剩 ~40GB。装模型权重前必须先清理或扩容,不然连一个视频模型 checkpoint 都下不下来。** `huntun` 账号下可见的大头:`shanhai` 74G、`models` 21G、`anaconda3` 8G、`visionary` 2.5G;剩下约 700GB 在其他用户目录 / 系统路径下,当前账号权限看不到,未深挖。
- 至少 5 个系统账号(huntun / hermes / zhangxiaobai / zhanghui / admin)——**这台机器同时被 visionary 和 shanhai 两个项目占用**,GPU 无自动化调度,纯人工协调排期。
- 工具链已装好,不用重装:`uv 0.12.5`、`bun 1.4.0`、`python 3.12.3`、`git 2.43.0`、`docker`。

## 端口(共享机 gx10-8e22,已占用,Kelvoy 部署时要避开)

| 端口 | 占用者 |
| --- | --- |
| 8000 | visionary-backend(FastAPI/uvicorn) |
| 5000 | shanhai-web |
| 5099 | comfyui-bridge |
| 8188 | ComfyUI |
| 6379 | 系统级 Redis(apt 装的,`redis` 系统用户跑的,不确定能否借用,需和机器归属方确认) |
| 11434 | 疑似 Ollama 默认端口(未验证,若属实 Kelvoy 的 LLM 环节或许能直接复用,留给 M0-4 细查) |

## 硬件与现状(2026-09-24 实地侦察,只读,专属机 spark-c327)

- **连接**:公网 `106.13.186.155:6063`(SSH),账号 `Developer`。密钥已装好:`ssh -i ~/.ssh/kelvoy_dgx -p 6063 Developer@106.13.186.155`,免密登录验证通过。远端密码登录目前仍然开着,没有主动关掉;密钥是唯一记进这份文档的凭证,密码不进 git。同一台机器也能在 Tailscale 网段看到一个地址(`100.122.65.26`),没有专门验证走这条路是否稳定,当前默认用公网 IP。
- GPU:NVIDIA GB10,驱动 580.126.09——与共享机同型号同驱动版本。
- **磁盘:3.7TB,已用 1.9TB,可用 1.7TB**。共享机"40GB 装不下模型权重"这个问题在这台上不存在。
- 内存使用率 16%,系统负载 0.08,机器基本闲置。
- 工具链:`uv 0.12.17`、`docker 28.5.1`、`python 3.14.7` 已装;**`bun` 未装**,部署前需要先装。

## 端口(专属机 spark-c327,已占用/在跑东西)

| 端口 | 占用者 |
| --- | --- |
| 22 | SSH |
| 80 | 未深挖是什么 |
| 8000 | 有进程在监听,未确认是谁的(按要求没有深查,只做了避让) |
| 8188 | `python3` 进程,大概率是 ComfyUI(和共享机同样的端口习惯,未逐一验证) |
| 631 | CUPS(系统自带,无关) |
| 9100 | 疑似 `node_exporter`(监控用,无关) |
| 53 | `systemd-resolved`(系统 DNS,无关) |
| 11000 | 只监听 `127.0.0.1`,未深挖 |

**Kelvoy 现有端口在两台机器上都确认无冲突,不用改代码**:`apps/web` 默认 `3000`、`services/inference` 已改的 `8100`,都不在共享机或专属机的占用列表里。

## 部署模式(参考 visionary,不是照搬)

visionary 用 systemd `--user` 服务 + `Linger=yes`,不需要 sudo——这个模式适合共享、无 root 权限的账号,Kelvoy 可以照搬。但 visionary 的人工运维通道是"入站"(后端绑 `0.0.0.0:8000`,人从 Tailscale 直接连进来),**这和 Kelvoy PRD v0.2 §9 的"worker 只出站连接"硬约束是两码事**——那条约束管的是生产流量(worker 拉队列),人工 SSH/运维访问走 Tailscale 入站不违反它。对外公网暴露如果需要,visionary 用的 Cloudflare Tunnel(DGX 主动建出站隧道)这部分可以直接抄。

容器编排(裸 Docker / systemd / 别的)、模型权重具体怎么管理,还没定,等 M0-4 选完模型再定。

## 待确认(不是我能替你定的)

- [x] Kelvoy 专属那台 DGX 的连接信息(IP/主机名、账号、密钥)——2026-09-24 拿到,见上方 spark-c327 一节。
- [ ] 40GB 可用空间怎么解决:清理旧数据,还是扩容/换盘?(共享的这台;专属机空间充足,M0-4 的模型评测可以优先考虑放专属机)
- [ ] 6379 的 Redis 能不能给 Kelvoy 用,还是要自己起一个换端口。(共享的这台)
- [ ] 11434 是不是 Ollama,能不能复用。(共享的这台)
- [ ] 专属机 8000/8188/80/11000 上跑的到底是什么、是不是别的项目也在用这台——只做了端口避让,没有深查进程归属,如果这台其实也不是"纯 Kelvoy 专属",PRD 的"两台对等"假设还要再想。
- [ ] 专属机的密码登录要不要关掉、只留密钥——目前两种方式都能登,没有主动收紧。
- [ ] PRD v0.2 §8/§9 里"两台对等、坏一台容量减半"的措辞要不要改成"一台专属(空间富裕)+ 一台共享(空间紧张、随时被抢 GPU)"——现在先不动 PRD 正文。
