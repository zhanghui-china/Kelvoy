# M0-4 Spark 模型评测：技术摸底（图像 + 视频）

对应 issue #4「M0-4 Spark 模型评测：2 图像 + 2 视频 + 1 国内 API」。

## 这份文档回答什么、不回答什么

**只回答**：这几个候选在 Spark（黑客松分配节点 spark-63）上跑不跑得动、多快、参考图条件生成对地标保真有没有实际帮助——纯技术可行性摸底，**不是可以直接拍板的最终结论**。

**局限（在结论之前必须先看这几条）**：
- 地标参考图是维基百科公开图库图（灵山大佛、迎客松、清名桥各一张），**不是 #1 目的地包的实拍图**。#1 还没做，真正的地标保真度评审要等实拍图出来后重测。
- 没有做跨镜/跨期一致性、漂移率测试——那是 #3 一致性评审的范围，需要真实角色资产和多镜序列，这次只测了单张图/单段视频。
- 国内 API 候选（StepFun）不在这份文档里——团队决定 StepFun 只用于 LLM/脚本生成环节，图像和视频全走本地模型，StepFun 的评测由另一项工作单独处理。
- 视频候选没有测 9:16 竖版分辨率（Kelvoy 目标输出格式），下面的视频结果用的是模型训练默认的 1344×768 横版——这是为了先验证"能不能跑通"，不是最终交付分辨率的验证。

## 环境

黑客松分配节点 `spark-63`（内部主机名 `spark-c327`，见 `infra/dgx/README.md`），GPU：NVIDIA GB10，统一内存 130GB。模型文件由队友 wuzi/zhanghui 预先下载在节点上，ComfyUI 常驻 `127.0.0.1:8188`。测试通过 ComfyUI 的 HTTP `/prompt` API 提交，没有复用队友手工搭的网页 workflow（没保存下来），根据 ComfyUI 官方模板 + 节点自带的 example workflow 重新拼的 API 版本。

## 结果总览

| 候选 | runs_on_spark | sec_per_shot | fail_rate | landmark_fidelity（网图初筛，非正式评审） | cost |
| --- | --- | --- | --- | --- | --- |
| Qwen-Image 2.1（纯文生图，对照组） | 是 | 34–42s | 0/3 | 灵山大佛这种超高知名度地标，纯文字描述已经生成得相当像——大概率是预训练数据里见过很多张实拍图；不能代表冷门/小众目的地的表现 | 黑客松节点资源，暂不计入本地成本对比，留待 M0-6 用生产环境实际算力单价回填 |
| Qwen-Image-Edit 2511（参考图条件生成） | 是 | 49s–784s，波动极大 | 0/3 | 参考图本身的地标细节保真度极高（部分结果和原图肉眼难分），但**当前参数下模型基本没有响应"加入人物"这条编辑指令**，只做了色调/对比度调整——这次没能验证出"保真 + 按指令编辑"两者兼得的真实上限，是参数问题，不是模型能力的定论 | 同上 |
| MiniMax H3（ref/first-frame → 视频） | 是 | 347s–603s | 0/2 | 首帧锚定机制下首帧地标 100% 保真（就是参考图本身）；后续帧是否漂移需要人工看完整视频，样片在远端节点上，见下方 | 同上 |
| LTX-Video 2.5 | **没跑** | — | — | — | — |

## 逐项细节

### Qwen-Image 2.1（对照组，不给参考图）
`qwen_image_2.1_int8_convrot.safetensors` + `qwen3vl_8b_bf16.safetensors` + `qwen_image_2.1_vae_bf16.safetensors`，25 步 euler/simple，1024×1024，prompt 里直接写地标中文名。三次全部成功，34–42 秒。生成结果里灵山大佛的姿势、法衣、莲花座细节都对，说明这类全国知名地标模型"认识"，仅靠文字描述就有不错的保真度——**这削弱了"必须靠参考图"这条论证对超知名地标的说服力，但 Kelvoy 首批目的地里南长街、拈花湾这类没有灵山大佛那么高辨识度，参考图条件生成的价值大概率在这些目的地上更明显**，这次没测这一层对比。

### Qwen-Image-Edit 2511（参考图条件生成，FR-04 硬约束的候选）
`qwen_image_edit_2511_int8_convrot.safetensors` + `qwen_2.5_vl_7b_fp8_scaled.safetensors` + `qwen_image_vae.safetensors`，参考图经 VAE 编码后以 denoise=0.85、cfg=2.5、20 步跑图生图。三次全部成功但耗时差异巨大：
- 灵山大佛（1110×1468 参考图）：114.8 秒
- 清名桥（800×552 参考图）：约 49 秒
- 迎客松（**3648×2736**，团队原图未压缩）：约 784 秒（13 分钟）

**工程发现**：参考图分辨率直接决定耗时，13 倍的分辨率差距对应 13 倍的耗时差距——生产环境喂给这个模型前必须先把参考图统一下采样到合理尺寸，不能让用户上传的原图直接进模型。

**质量发现**：三次生成结果里地标本身的还原度都很高（清名桥那张连石碑上"大运河 清名桥"的字都清晰保留），但没有一次真正显示出 prompt 要求的"游客站在前景"——模型基本只是复刻原图加了色调滤镜。这说明这次的采样参数（denoise/cfg）偏保守，验证了"能保真"但没验证"能按指令改"，这是本次评测最大的技术缺口，需要后续用更高 denoise 或换一种参考图注入方式（比如 ControlNet 式结构参考而非直接图生图起点）重测。

样片：`~/ComfyUI/output/KelvoyEval/qwen_edit_0000{1,2,3}_.png`（远端节点），本地留档在 `/private/tmp/.../scratchpad/kelvoy-eval/outputs/`（会话临时目录，非仓库路径）。

### 更正：上一轮"团队昨天验证过 Turbo LoRA、213-300秒"的说法不成立

复核 `~/ComfyUI/user/comfyui_8188.prev2.log` 完整日志后发现：2026-09-23 16:16-16:26 那两次 MiniMax H3 记录——300.51 秒那次是**被人工手动中断**的（日志明确写 `Interrupting prompt`），213.91 秒那次是**跑完采样后在存视频这一步崩溃**的（`TypeError: can't convert cuda:0 device type tensor to numpy`，`VHS_VideoCombine` 节点报错，没有产出视频文件）。这两次用的都是不带 LoRA 的裸模型（`minimax_h3_fl2va_pruned_int8_convrot.safetensors`），全部日志文件里搜不到任何一次 LoRA 节点被加载的记录。上一版结论里"团队用 Turbo LoRA 验证过 213-300 秒"的说法是误判，**没有这回事**——予以更正。

### MiniMax H3（首帧生视频）
`minimax_h3_fl2va_pruned_int8_convrot.safetensors` + `qwen3vl_32b_minimax_h3_int8_convrot.safetensors` + `minimax_h3_video_vae_int8_convrot.safetensors`/`minimax_h3_audio_vae_fp32.safetensors`，用 `MiniMaxH3ImageToVideo` 把地标参考图设为 first_frame，`MiniMaxH3DualClockSamplerT8` 8 步采样（`dual_clock_euler`），1344×768，124 帧（~5s）。两次全部成功：
- 灵山大佛：603.1 秒
- 清名桥：346.5 秒

**注意**：这次**没有用团队昨天验证过的 Turbo LoRA 加速**——示例 workflow 引用的 lora 文件名（`minimax_h3_turbo_4步加速ema_comfyui.safetensors`）在节点上找不到完全对应的文件，为了保证这次摸底先把"能不能跑通"这条主线跑稳，没有替换成近似版本去赌兼容性。所以这次的耗时（347–603 秒）比团队昨天用 Turbo LoRA 跑出来的（213–300 秒）慢，**不代表 MiniMax H3 的产品化速度上限**——后续要接上正确版本的 4-step turbo lora（节点上有 `minimax_h3_fl2v_turbo_4step_v1.2_768p_comfyui_bf16.safetensors` 等几个候选版本）重测才有意义。

首帧是参考图本身（锚定机制决定的，不是生成的），所以首帧地标保真度是 100%；完整视频里地标有没有随镜头运动漂移、变形，需要真人看一遍 mp4，样片在远端节点：`~/ComfyUI/output/KelvoyEval/minimax_h3_0000{1,2}.mp4`，本地留了首帧截图在 scratchpad。**这次没测 9:16 竖版**，只用了模型训练默认的横版分辨率验证跑通与否。

### LTX-Video 2.5（完全没跑）
本地只下载了 transformer 主干权重（`ltx-2.5-22b-distilled-transformer-comfy-int8-convrot.safetensors`，约 20GB）。官方 ComfyUI-LTXVideo 节点包自带的示例 workflow（`LTX-2.5_T2V_I2V_Single_Stage_Distilled.json`）明确要求额外三个文件：专用视频 VAE（`ltx-2.5-video-vae-*.safetensors`）、专用音频 VAE（`ltx-2.5-audio-vae-bf16.safetensors`）、专用文本编码器（`gemma4-12b-with-proj-ltx-2.5-bf16.safetensors`）——这三个节点上完全没有。

这不是"显存不够"或"报错崩溃"，是**环境不完整**：要跑通大概还需要再下载 30GB+ 的模型文件。这次摸底为了控制时间没有额外下载，留给后续需要正式测 LTX-Video 时再补。

## 第二轮跟进（补 Turbo LoRA 重测 + LTX-Video 缺文件）—— 本轮中断，两项均未拿到有效结果

### MiniMax H3 接 Turbo LoRA：提交成功但过程中节点失联，无有效数据

`~/models/loras/` 下确认存在多个 MiniMax H3 Turbo LoRA 变体（4-step/8-step，v0.1-v1.2 各版本）。用上一轮跑成功的 workflow（ComfyUI `/history` 接口取回的真实 graph，8 步、无 LoRA、1344×768、124 帧）为基础，插入 `LoraLoaderModelOnly` 节点接 `minimax_h3_fl2v_turbo_4step_v1.2_768p_comfyui_bf16.safetensors`，采样步数从 8 降到 4，其余参数不变，用清名桥参考图重新提交。

第一次提交后 ComfyUI 进程被重启（`tmux` 里 `comfyui` 会话时间戳显示 02:55:39 重新创建，不是我做的，大概率是同时在用这台节点的队友），任务连同队列记录一起丢失。重新提交后任务正常进入 `queue_running`，跑到第 138 秒时——**整个节点通过 SSH 完全无法连接了（`Connection refused`，端口 6063），连续重试 5 分钟以上没有恢复**。不清楚是节点崩溃、跳板机转发出问题，还是别的原因；这段时间里我没有执行任何可能导致节点重启/崩溃的操作（没有 reboot、没有改系统配置）。

**结论：这次没有拿到 Turbo LoRA 版本的有效耗时数据**——138 秒时任务还在跑（比对照组同一节点数尚未跑完的进度合理），既不能证实也不能证伪"接 LoRA 更快"。需要等节点恢复后重新测一次。

### LTX-Video 2.5 缺的文件：已定位下载地址并启动下载，结果未知

在 `ComfyUI-LTXVideo` 节点包自带的 `example_workflows/2.5/LTX-2.5_T2V_I2V_Single_Stage_Distilled.json` 里找到了官方给的确切文件名和 HuggingFace 地址：
- `ltx-2.5-audio-vae-bf16.safetensors`（音频 VAE）
- `ltx-2.5-video-vae-conv-bf16.safetensors`（视频 VAE，选了官方标注"更快/更省显存"的卷积解码版本，不是质量更好但更重的扩散解码版本）
- `gemma4-12b-with-proj-ltx-2.5-bf16.safetensors`（LTX-2.5 专用文本编码器，必需）

没下可选的 `gemma4_e2b_it_bf16.safetensors`（官方文档写明这个是"prompt 自动增强"用的文本增强器，默认可关，不影响基本生成能力，为了省时间和带宽跳过了）。

用 `wget -c` 在节点上的 `tmux` 会话（会话名 `ltx_dl`）里后台下载这三个文件到 `~/models/vae/` 和 `~/models/text_encoders/`，完成后会在 `~/ltx_download_done.flag` 写一个标记。提交下载后不久就遇到上面那次节点失联，**下载有没有完成、`ltx_dl` 这个 tmux 会话还在不在，都无法确认**——`tmux` 会话本身是服务端持久的，节点只要没有真的重启/崩溃，理论上下载会话应该还在跑或者已经跑完，但这需要节点恢复联通后有人上去确认（`tmux attach -t ltx_dl` 或者直接看 `~/ltx_download_done.flag` 存不存在、`~/models/vae/`、`~/models/text_encoders/` 里文件是否完整）。

### 这轮新发现的阻塞项（比模型选型更紧急，两条都要人处理）

**1. 节点被反复重启，Turbo LoRA 测试始终没能跑完。** SSH 那次 5 分钟失联后来自己恢复了，但恢复后 ComfyUI 进程在约 25 分钟内至少重启了 3 次（tmux `comfyui` 会话时间戳：02:55:39 → 03:08:28 → 03:19:03），每次重启都把队列里正在跑的任务直接冲掉。两次重新提交的 Turbo LoRA 任务都在跑到一半时被这样冲掉，**始终没能拿到一次完整的、成功或失败都算数的结果**。这台节点大概率同时有队友在用、在重启 ComfyUI 做他们自己的测试——不是我这边能控制的资源冲突，继续在这个时间点上重试性价比很低，先记录下来，等确认没人跟我抢的时间段再测。

**2. LTX-Video 缺的文件下不下来，原因比"网络不通"更根本：`Lightricks/LTX-2.5` 是一个 HuggingFace 门禁仓库（gated repo）。** 先是发现 `huggingface.co` 从这个节点直接连不通（DNS 能解析但连接超时/网络不可达，这也是为什么 `.bashrc` 里团队自己配了 `HF_ENDPOINT=https://hf-mirror.com`）；换成镜像站 `hf-mirror.com` 后连接倒是通了，但三个文件全部返回 **403 Forbidden**——查了 LTX-2.5 官方示例 workflow 里的原话："Make sure to click the 'Agree and Access' button the first time you download"，说明这个仓库需要先用一个 HuggingFace 账号在网页上点击同意协议，再拿这个账号生成的 access token 才能下载，匿名 `wget` 无论走哪个域名都下不了。**这需要人去 HuggingFace 网站上用账号点一下"Agree and Access"、生成一个 token，我这边没有这个操作权限。**

## 更新：回答 issue #4 的四个验收问题（第二轮跟进后，仍然初步）

第二轮跟进没有拿到任何新的、可以改变结论方向的数据——Turbo LoRA 因为节点被反复重启始终没测完，LTX-Video 卡在需要人工到 HuggingFace 网站授权这一步。上一节"回答 issue #4 的四个验收问题"的四条结论和"还需要做的四件事"原样有效，不重复。要加两条：**(5) LTX-Video 需要先有人去 HuggingFace 网页授权 `Lightricks/LTX-2.5` 并生成 token，不是纯技术问题；(6) MiniMax H3 的 Turbo LoRA 重测需要挑一个节点没有别人在用的时间段，不然会一直被并发重启打断。**

## 回答 issue #4 的四个验收问题（初步，非最终）

- **图像留本地还是走 API**：本地明显可行（Spark 跑得动，30-50 秒级），但这次没测国内图像 API 做对照，无法下结论哪个更划算/更快；本地至少证明了"跑得动"这条门槛。
- **视频留本地还是走 API**：本地可行但慢（347–603 秒一段 5 秒视频，且没用加速 LoRA），PRD 风险表原本就预判"视频环节大概率长期走 API"——这次的本地耗时数据支持这个预判方向，但需要接上 Turbo LoRA 重测才能给出更接近生产的数字。
- **单期 GPU 分钟目标**：这次没有覆盖到"一期 24-30 镜全部跑一遍"的规模，只做了单镜/单张验证，还不能回填这个数字。
- **整期全走 API 多少钱 vs ¥20**：本地测试不产生这个数字，需要国内 API 候选（视频）的实际报价对照，这次没测。

**这四条都还没有到能定论的地步**——M0-4 要给出完整回答，还需要：(1) 接上 Turbo LoRA 重测 MiniMax H3 的真实速度；(2) 补齐 LTX-Video 缺的模型文件再测一次；(3) 用 #1 的实拍参考图重做一遍图像/视频的保真度评审；(4) 找一个国内视频生成 API 做实际报价对照（issue 原文要求"1 国内 API"，团队决定 StepFun 只做 LLM，图像/视频国内 API 对照目前是空的，需要另外决定测哪家或者明确"不测，全走本地+开源"这个结论）。
