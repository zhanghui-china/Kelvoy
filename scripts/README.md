# scripts/

Utility scripts (e.g. a future `deploy-dgx.sh`, an OpenAPI export helper once
the FastAPI app exists) will live here, mirroring visionary's `scripts/`. It
is intentionally empty of actual scripts for now — nothing to deploy or
export yet.
