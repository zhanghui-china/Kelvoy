# Kelvoy Architecture

> Placeholder. This document will track the as-built system architecture
> once the backend, frontend, and DGX bridge have real logic — mirroring
> visionary's `docs/architecture.md` role (implementation-status sync doc,
> not a design doc). See `docs/decisions/` for standing decisions made so
> far.

TBD.
